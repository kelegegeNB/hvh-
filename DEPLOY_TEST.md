# 部署验证测试用例

本文档用于验证 `deploy_all.sh` 脚本在不同环境下的部署稳定性和可靠性。

## 测试环境
- **开发环境**: 本地 Docker Desktop (Windows/Mac) 或 Linux VM。
- **生产环境**: 云服务器 (Ubuntu/CentOS)，预装或未装 Docker。

## 测试用例列表

| ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |
|----|---------|----------|----------|----------|
| **T01** | **全新部署** | 无 `.env` 文件，无 Docker 容器运行 | 运行 `./deploy_all.sh` | 1. 自动生成 `.env` (包含 `JWT_SECRET`, `ADMIN_KEY`)<br>2. Docker 镜像构建成功<br>3. 容器 `backend`, `frontend`, `db` 状态为 `Up`<br>4. 数据库迁移成功 (`db:push`)<br>5. 输出访问地址和密钥 |
| **T02** | **增量部署 (代码更新)** | 服务已运行，本地有代码变更 | 运行 `./deploy_all.sh` | 1. 自动备份 `.env`<br>2. 拉取最新代码 (Git)<br>3. 重建镜像并重启容器<br>4. 数据库结构同步<br>5. 旧镜像被清理 |
| **T03** | **环境修复 (依赖缺失)** | 卸载 `docker-compose-plugin` (Linux) | 运行 `./deploy_all.sh` | 1. 脚本检测到缺失组件<br>2. 自动安装成功<br>3. 部署流程继续并完成 |
| **T04** | **回滚测试 (构建失败)** | 修改 `backend/Dockerfile` 引入语法错误 | 运行 `./deploy_all.sh` | 1. 构建步骤报错<br>2. 触发 `trap` 捕获错误<br>3. 执行回滚逻辑 (恢复配置、停止坏容器)<br>4. 脚本以非 0 状态码退出 |
| **T05** | **回滚测试 (健康检查失败)** | 修改后端代码使其启动时 Crash | 运行 `./deploy_all.sh` | 1. 容器启动<br>2. 健康检查循环检测失败 (超时)<br>3. 触发回滚<br>4. 输出错误日志 |
| **T06** | **数据持久化验证** | T01 完成后，在数据库写入一条数据 | 执行 T02 部署 | 1. 部署完成后，查询数据库<br>2. T01 写入的数据依然存在 (验证 Volume 挂载) |

## 手动验证步骤

1.  **检查日志**:
    ```bash
    cat deploy.log
    ```
    确认无 `ERROR` 级别日志。

2.  **检查容器状态**:
    ```bash
    docker compose ps
    ```
    所有服务状态应为 `Up` (healthy)。

3.  **访问前端**:
    浏览器打开 `http://localhost` (或服务器 IP)，确认 404 页面动画效果及主页正常加载。

4.  **访问后台 API**:
    `curl http://localhost:3000/api/health` (假设有健康检查接口) 或尝试登录后台。
