# 自动部署脚本使用指南

`deploy.sh` 是一个集成了环境检查、备份、配置、构建、发布及数据库迁移功能的自动化部署脚本。

## 功能特性

- **一键部署**: 单个脚本完成全流程。
- **环境自适应**: 自动检测并安装 Docker/Docker Compose (Linux)。
- **安全配置**: 自动生成 `JWT_SECRET` 和 `ADMIN_KEY`。
- **自动配置**: 自动生成和更新 `.env` 配置文件。
- **数据库迁移**: 智能等待数据库启动并执行 Schema 同步 (`db:push`)。
- **清理优化**: 自动清理旧的 Docker 镜像，节省磁盘空间。
- **日志记录**: 所有操作记录在 `deploy.log`。

## 包含的系统功能 (基于当前代码)

部署后的系统包含以下核心功能：
- **黑信榜/曝光台**: 
  - 支持 **搜索** (按标题、ID、发布者IP)。
  - 支持 **分页** 加载，提升性能。
  - **IP 隐私保护**: 自动隐藏敏感 IP 段，且仅显示 IPv4。
- **管理后台**:
  - **仪表盘**: 用户、公告、广告、音乐管理。
  - **日志审计**: 
    - **操作日志**: 记录管理员的所有敏感操作 (含中文动作翻译)。
    - **访问日志**: 记录曝光页面的访问详情 (支持 CSV 导出)。
    - **用户日志**: 记录普通用户的行为。
  - **投诉处理**: 完善的投诉反馈流程。
- **系统设置**:
  - **广告管理**: 首页侧边栏广告配置。
  - **公告管理**: 首页弹窗公告 (支持"今日不再提示")。
  - **背景音乐**: 支持网易云/MP3直链，含自动播放尝试机制。

## 快速开始

### 1. 赋予执行权限
在 Linux/Mac 环境下，首次使用需赋予脚本执行权限：
```bash
chmod +x deploy.sh
```

### 2. 运行部署
```bash
./deploy.sh
```

### 3. 查看日志
部署过程中的详细信息和错误会记录在当前目录下的 `deploy.log` 文件中：
```bash
tail -f deploy.log
```

## 配置说明

脚本会自动读取或创建 `.env` 文件。你可以手动修改该文件来调整配置：

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `DATABASE_URL` | 数据库连接字符串 | 自动构建 (默认 postgresql://hvh:...) |
| `JWT_SECRET` | JWT 签名密钥 | 自动生成的 32 字节 Hex |
| `ADMIN_KEY` | 管理员系统密钥 | 自动生成的 16 字节 Hex |
| `DB_PORT` | 数据库端口 | `5432` |
| `PORT` | 后端端口 | `3001` |

## 故障排查

如果部署失败：
1.  **检查日志**: 查看 `deploy.log` 中的 `[ERROR]` 信息。
2.  **检查容器**: 运行 `docker compose logs` 查看容器内部报错。
3.  **数据库连接**: 如果 `db:push` 失败，请检查 `DATABASE_URL` 是否正确，以及数据库容器是否正常运行 (`docker compose ps`)。
